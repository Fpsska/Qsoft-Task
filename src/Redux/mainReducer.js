const initialState = {
    profileName: "Константин"
}

const mainReducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state
    }
}

export default mainReducer;